<%= link_to edit_card_path(card), id: dom_id(card), class:"bg-base-100 flex justify-between items-center w-full p-3 rounded-field", data: { controller: "draggable moveable", turbo_frame: "modal" } do %>
  <div class="flex">
    <%= card.title %>
  </div>
<% end %>
