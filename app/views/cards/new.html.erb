<% content_for :title, "New card" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New card</h1>

  <turbo-frame id="modal">
    <%= render "form", card: @card %>
  </turbo-frame>

  <%= link_to "Back to board", board_path(@card.board), class: "btn" %>
</div>
