<% content_for :title, "New board" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New board</h1>

  <turbo-frame id="modal">
    <%= render "form", board: @board %>
  </turbo-frame>

  <%= link_to "Back to boards", boards_path, class: "btn" %>
</div>
